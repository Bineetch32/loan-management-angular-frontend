import {Component,OnInit} from '@angular/core';import {CommonModule} from '@angular/common';import {RouterLink} from '@angular/router';import {FormsModule} from '@angular/forms';import {LoanService} from './loan.service';import {Loan} from './models';
@Component({selector:'app-loans',standalone:true,imports:[CommonModule,RouterLink,FormsModule],templateUrl:'./loans.component.html'})
export class LoansComponent implements OnInit{loans:Loan[]=[];filtered:Loan[]=[];search='';constructor(private service:LoanService){}ngOnInit(){this.load()}load(){this.service.getLoans().subscribe(x=>{this.loans=x;this.filter()})}filter(){const q=this.search.toLowerCase();this.filtered=this.loans.filter(x=>x.customerName.toLowerCase().includes(q)||x.loanType.toLowerCase().includes(q))}approve(id:number){this.service.approve(id).subscribe(()=>this.load())}reject(id:number){this.service.reject(id).subscribe(()=>this.load())}sanction(id:number){this.service.sanction(id).subscribe(()=>this.load())}remove(id:number){if(confirm('Delete this loan?'))this.service.delete(id).subscribe(()=>this.load())}}